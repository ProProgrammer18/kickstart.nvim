vim.schedule(function() require('lazy.core.config').options.change_detection.notify = false end)
vim.schedule(function() require('lazy.core.config').options.checker.notify = false end)
vim.schedule(function() require('lazy.core.config').options.checker.enabled = true end)
